# hacker-house-medellin-libs

Reusable community, application, stay, room, event, project, and authorization libraries.

Initialized through `DEN-1950` as a testable `libs` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
